namespace GoodNoodle.Domain;

public static class Enums
{
    public enum UserStatus { Pending, Accepted, Declined }
    public enum GroupRole { Teacher, Student }
    public enum UserRole { Admin, User }
}
